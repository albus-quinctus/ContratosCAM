{
  "name": "contratoscam",
  "version": "0.1.0",
  "description": "Transparencia en la contratación pública de la Comunidad de Madrid",
  "author": "Albus Quinctus",
  "license": "MIT",
  "homepage": "https://github.com/albus-quinctus/ContratosCAM",
  "repository": {
    "type": "git",
    "url": "https://github.com/albus-quinctus/ContratosCAM.git"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "type": "module",
  "scripts": {
    "download": "node scripts/download.js",
    "download:full": "node scripts/download.js --full",
    "download:ted": "node scripts/download-ted.js",
    "parse": "node scripts/parse.js",
    "parse:ted": "node scripts/parse-ted.js",
    "transform": "node scripts/transform.js",
    "import-db": "node scripts/import-db.js",
    "validate": "node scripts/validate.js",
    "enrich:nif": "node scripts/enrich-nif.js",
    "etl": "npm run download && npm run parse && npm run transform && npm run import-db",
    "etl:ted": "npm run download:ted && npm run parse:ted",
    "etl:all": "npm run download && npm run download:ted && npm run parse && npm run parse:ted && npm run transform && npm run import-db",
    "etl:validate": "npm run etl:all && npm run validate",
    "etl:full": "npm run etl:all && npm run validate && npm run enrich:nif",
    "serve": "npx serve . --listen 3000",
    "dev": "npm run serve"
  },
  "dependencies": {
    "fast-xml-parser": "^5.9.3",
    "sql.js": "^1.12.0"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  },
  "keywords": [
    "transparencia",
    "contratacion-publica",
    "datos-abiertos",
    "comunidad-de-madrid",
    "open-data"
  ]
}
